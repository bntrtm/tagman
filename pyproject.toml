[project]
name = "tagman"
version = "0.1.0"
description = "Application providing an efficient and user-friendly GUI for tagging PNG datasets with TXT files."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.1.1",
    "tk>=0.1.0",
]

